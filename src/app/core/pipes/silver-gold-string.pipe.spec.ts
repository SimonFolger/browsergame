import { SilverGoldStringPipe } from './silver-gold-string.pipe';

describe('SilverGoldStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SilverGoldStringPipe();
    expect(pipe).toBeTruthy();
  });
});
